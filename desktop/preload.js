'use strict';

// Intentionally empty. The hosted game uses the desktop=1 query flag and does
// not need privileged renderer APIs. Keeping this preload inert preserves the
// context-isolated, sandboxed renderer boundary.
